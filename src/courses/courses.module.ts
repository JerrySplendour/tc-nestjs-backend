import { Module } from '@nestjs/common'; import { TypeOrmModule } from '@nestjs/typeorm'; import { Certificate, Course, CourseModule, Lesson, LessonCompletion } from '../database/entities'; import { CoursesController } from './courses.controller'; @Module({ imports: [TypeOrmModule.forFeature([Course, CourseModule, Lesson, LessonCompletion, Certificate])], controllers: [CoursesController] }) export class CoursesModule {}
