import { Body, Controller, Get, Param, Post, Query, UseGuards } from '@nestjs/common'; import { JwtAuthGuard, Roles, RolesGuard } from '../common/auth'; import { ConfirmMediaDto, MediaQueryDto, PresignedUrlDto } from './media.dto'; import { MediaService } from './media.service'; @Controller('media') @UseGuards(JwtAuthGuard) export class MediaController { constructor(private readonly media: MediaService) {} @Post('presigned-url') @UseGuards(RolesGuard) @Roles('admin') presign(@Body() dto: PresignedUrlDto) { return this.media.presign(dto); } @Post('confirm') @UseGuards(RolesGuard) @Roles('admin') confirm(@Body() dto: ConfirmMediaDto) { return this.media.confirm(dto); } @Get(':type') list(@Param('type') type: string, @Query() query: MediaQueryDto) { return this.media.list(type, query.page, query.per_page); } }
