import { Module } from '@nestjs/common'; import { TypeOrmModule } from '@nestjs/typeorm'; import { Event, EventRegistration, EventReview, User } from '../database/entities'; import { EventsController } from './events.controller'; @Module({ imports: [TypeOrmModule.forFeature([Event, EventRegistration, EventReview, User])], controllers: [EventsController] }) export class EventsModule {}
